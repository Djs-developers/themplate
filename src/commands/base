const Discord = require("discord.js");
const { SlashCommandBuilder } = require('@discordjs/builders');

module.exports = {
  data: new SlashCommandBuilder()
    .setName('')
    .setDescription(''),
  perms: [],
  disabled: false,

  interactionRun: async (client, interaction, server) => {

    
    
  },

  messageRun: async (client, message, args, server) => {

    
    
  }
};